# Story 6.2b — Phase 3 Handoff

**For:** a fresh Claude Code session to implement Phase 3.
**Date:** 2026-05-15
**Story file:** `docs/stories/6.2b.story.md` (read its Phase 3 AC + Dev Agent Record)

## Status

- **Phase 1** (operator env) — DONE. 5 vars set in Vercel prod: `TRANSCRIPTION_PROVIDER=deepgram`, `DEEPGRAM_API_KEY`, `AI_PROVIDER=openai`, `OPENAI_API_KEY`, `RECALL_REALTIME_WEBHOOK_SECRET`.
- **Phase 2** (live streaming transcription) — DONE, merged to `main`, validated (`tsc` + ESLint + 23 unit tests).
- **Phase 3** (audio playback + synced transcript) — NOT STARTED. This handoff.

## First steps for the fresh session

1. Branch off main: `git checkout main && git pull && git checkout -b feat/6.2b-phase3-audio-playback`
2. Read `docs/stories/6.2b.story.md` — Phase 3 Acceptance Criteria + Dev Agent Record.
3. Activate the BMad dev agent (`/dev`), then implement.

## Phase 3 scope (from story AC)

1. `apps/web/src/app/api/sessions/[id]/audio-url/route.ts` — GET `{ url, expires_at }`. If `sessions.transcript_audio_url` is expired (HEAD → 403), re-fetch via Recall `GET /recording/{recording_id}` → update DB → return fresh URL.
2. `apps/web/src/components/sessions/TranscriptAudioPlayer.tsx` — HTML5 `<audio>`, `forwardRef` exposing `seek(ms)`, `onTimeUpdate(ms)` callback.
3. `apps/web/src/components/sessions/SyncedTranscriptView.tsx` — clickable chunks → `onSeek(start_ms)`; active chunk (`start_ms <= t <= end_ms`) gets `bg-[#FEF3C7]` highlight + auto-scroll.
4. `apps/web/src/components/sessions/SessionDetailSkeleton.tsx`.
5. Session detail page `apps/web/src/app/(dashboard)/clients/[id]/sessions/[sessionId]/page.tsx` — **ALREADY EXISTS — MODIFY, do not create.** Layout: `grid-cols-3` on lg+ (transcript 2/3, sidebar 1/3 = summary + action items).
6. Cypress E2E — seed session w/ 3 chunks + mock audio URL → click middle chunk → assert `audio.currentTime` + highlight class.

## Critical facts (corrected vs the original story draft)

- `TranscriptChunk` = `{ speaker: number; speaker_name: string | null; text: string; start_ms: number; end_ms: number }` — NO `is_final`. Imported from `@meetsolis/shared`.
- Route param is `[id]` (the client id) — NOT `[clientId]`.
- `normalizeChunks(chunks)` in `apps/web/src/lib/services/recall/process-transcript-chunk.ts` — sorts + dedups; use for any transcript render. `speakerLabel(chunk)` and `concatTranscriptText(chunks)` also there.
- `sessions.transcript_chunks` (JSONB) — stored chunks. `sessions.transcript_audio_url` — recording URL (set on `recording.done`). `sessions.recall_session_id` — FK to `recall_sessions`.
- `recall_sessions.recording_id` — Recall recording id, source for the audio-url refresh endpoint.
- Migration `026_realtime_transcript.sql` already added all columns + the `append_transcript_chunk` RPC. No new migration unless Phase 3 needs more.
- Recall signed audio URLs: docs expose `expires_at` per URL, no fixed TTL — re-fetch from the API, do not hard-cache; read `expires_at`.
- CSP: verify `media-src` allows Recall S3 domains (`*.recall.ai`, `*.s3.amazonaws.com`) — see story Operator Notes Phase 3.
- Existing session API pattern: `apps/web/src/app/api/sessions/[id]/route.ts` (UUID regex, Clerk `auth()`, `getInternalUserId`, ownership via `.eq('user_id', userId)`). Recall API client: `apps/web/src/lib/services/recall/recall-client.ts` (`getRecallRecording`).

## Validation (run from `apps/web/`)

- `npm run type-check`
- `npm test`
- `npm run lint`

## Open items carried over

- Phase 2 integration test (5 mock events → DB state) — deferred, needs test-DB infra not present in repo.
- Phase 2 not yet live-tested — operator should run a real ~30s meeting and confirm a real transcript appears (Phase 1 verification AC).
- Recall `deepgram_streaming` — confirm at first live test whether it needs a Deepgram key configured inside Recall.
