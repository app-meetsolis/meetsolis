# Story 3.2: Manual Transcript Upload

## Status
Done

## Story

**As a** executive coach,
**I want** to upload my session transcript as a text file or docx, or paste it directly,
**so that** I can log a coaching session in under 2 minutes without any technical complexity.

## Acceptance Criteria

1. Accepted file types: `.txt` (direct text read) and `.docx` (parsed via mammoth to plain text)
2. Maximum file size: 25MB; error shown if exceeded
3. Paste/type text option: textarea supporting up to 50,000 characters with counter
4. Session date picker (required, defaults to today's date)
5. Session title input (required, e.g., "Session 5 — Leadership Transition")
6. Validation: at least one of (file upload OR pasted text) must be provided
7. Unsupported file type shows clear error message
8. File uploaded to Supabase Storage at `transcripts/[userId]/[sessionId]/[filename]`
9. Pasted text or extracted file text stored in `sessions.transcript_text`
10. File URL stored in `sessions.transcript_file_url`
11. On save: session created with `status = 'pending'`; AI summarization triggered automatically (POST to `/api/sessions/[id]/summarize`)
12. Success: modal closes, session card appears in timeline with "Processing..." state
13. Error states handled: file too large, wrong format, upload failed, network error

## Tasks / Subtasks

- [x] **Transcript Parsing Utility**
  - [x] Create `apps/web/src/lib/files/parseTranscript.ts`
  - [x] `.txt`: read as UTF-8 string
  - [x] `.docx`: `mammoth.extractRawText({ arrayBuffer })` → plain text
  - [x] Returns: `{ text: string, sourceType: 'txt' | 'docx' | 'paste' }`
  - [x] Install `mammoth` package if not already installed

- [x] **Session Upload API**
  - [x] **Extend** `POST /api/sessions` (currently JSON-only) to detect `Content-Type: multipart/form-data` and parse it — JSON requests must continue to work (backwards compat)
  - [x] Parse multipart: extract `client_id`, `session_date`, `title`, `transcript_text` (text fields) + optional file
  - [x] Validate ownership: client belongs to authenticated user
  - [x] Check transcript quota: `checkTranscriptLimit(userId)` — throws if limit exceeded
  - [x] Upload file to Supabase Storage (if file provided)
  - [x] Create session record
  - [x] Increment transcript count: `incrementTranscriptCount(userId)`
  - [x] Trigger summarization: `POST /api/sessions/[id]/summarize` (fire-and-forget, returns 202 stub)
  - [x] Return: created session record (201)

- [x] **SessionUploadModal — Tab 1: Manual Upload**
  - [x] File input accepting `.txt` and `.docx` only
  - [x] File size validation (client-side: reject >25MB)
  - [x] OR: textarea for paste input with character counter (50,000 max)
  - [x] Date picker (defaulting to today)
  - [x] Title input (required)
  - [x] Submit button: "Upload & Process"
  - [x] Loading state during upload

- [x] **Usage Limit Stubs** (real enforcement in Story 4.4)
  - [x] Create `apps/web/src/lib/quota/transcriptQuota.ts` with stub functions:
    - `checkTranscriptLimit(userId)` — always passes for now (no-op)
    - `incrementTranscriptCount(userId)` — no-op stub
  - [x] Show UpgradeModal if limit exceeded (wire to stub — always passes for now)

## Dev Notes

- `mammoth` is pure JS/TS, no native dependencies — safe for serverless
- Supabase Storage: use service role key for server-side uploads
- **Multipart parsing**: `POST /api/sessions` currently uses `request.json()` — must detect content-type and branch: if `multipart/form-data` use `request.formData()`; else `request.json()`. Existing JSON callers must not break.
- `SessionCreateSchema` already includes `transcript_text` (max 50k chars), `transcript_file_url`, `transcript_audio_url` — validate after parsing
- Summarization stub (`POST /api/sessions/[id]/summarize`) returns 202 immediately — client polls session status via React Query refetch interval on `GET /api/sessions/[id]`
- `checkTranscriptLimit` / `incrementTranscriptCount` are stubs in 3.2 — real enforcement deferred to Story 4.4

## Dependencies

- Story 3.1 (sessions API exists) — required ✅
- Story 4.4 (usage enforcement) — `checkTranscriptLimit` function; stubbed in this story

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- Supabase Storage bucket `transcripts` must be created manually before QA (public bucket or with RLS policy)
- If Storage upload fails, session is still created with transcript_text — graceful degradation
- `parseTranscript.ts` runs client-side; uses `arrayBuffer` API (browser) — not called server-side
- `mammoth` already in package.json — no new deps needed
- `SessionTimelineStub` now requires `clientId` prop — updated call site in client detail page

### File List
- `apps/web/src/lib/files/parseTranscript.ts` — NEW
- `apps/web/src/lib/quota/transcriptQuota.ts` — NEW
- `apps/web/src/components/sessions/SessionUploadModal.tsx` — NEW
- `apps/web/src/components/clients/SessionTimelineStub.tsx` — MODIFIED (wired modal, added clientId prop)
- `apps/web/src/app/api/sessions/route.ts` — MODIFIED (multipart support, Storage upload, summarize trigger)
- `apps/web/src/app/(dashboard)/clients/[id]/page.tsx` — MODIFIED (pass clientId to SessionTimelineStub)

### Change Log
- Created transcript parsing utility (txt + docx via mammoth)
- Created quota stub functions (checkTranscriptLimit, incrementTranscriptCount)
- Extended POST /api/sessions to handle multipart/form-data with backwards-compat JSON fallback
- Added Supabase Storage file upload with graceful failure handling
- Added fire-and-forget summarize trigger after session creation
- Created SessionUploadModal with file upload + paste modes, validation, loading state
- Enabled Upload button in SessionTimelineStub — wired to modal

## QA Results

### Review Date: 2026-03-13

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Solid implementation. Multipart/JSON dual-mode parsing is clean and backwards-compatible. The .docx server-side parsing fix (moving mammoth to Node.js runtime) was a necessary architectural correction caught during testing. Graceful degradation on Storage failure is correct. Minor issues found and fixed during review.

### Refactoring Performed

- **File**: `apps/web/src/components/sessions/SessionUploadModal.tsx`
  - **Change**: Moved `todayISO()` from inside component body (after early return) to module level
  - **Why**: Function defined after `if (!isOpen) return null` was a code smell — technically hoisted but misleading placement
  - **How**: Module-level utility is clearer and avoids confusion about initialization order

- **File**: `apps/web/src/app/api/sessions/route.ts`
  - **Change**: Added server-side 25MB file size guard before quota check
  - **Why**: Client-side validation only is insufficient — direct API callers can bypass it
  - **How**: `file.bytes.byteLength > MAX_FILE_BYTES` check returns 400 before any DB/Storage ops

- **File**: `apps/web/src/components/clients/SessionTimelineStub.tsx`
  - **Change**: Removed redundant `setIsModalOpen(false)` from `onSuccess` handler
  - **Why**: `SessionUploadModal.handleSubmit` already calls `onClose()` after `onSuccess()` — double-close was harmless but confusing
  - **How**: `onSuccess` now only fires the parent callback; modal close is handled by `onClose` prop

### Compliance Check

- Coding Standards: ✓ Types in packages/shared, config object for env vars, service role key pattern consistent
- Project Structure: ✓ `components/sessions/`, `lib/files/`, `lib/quota/` all correctly placed; PascalCase components
- Testing Strategy: ✓ Manual tests executed and passing; no automated tests (consistent with project phase)
- All ACs Met: ✓ ACs 1–11, 13 fully met. AC12 ("session card appears in timeline") intentionally partial — SessionTimelineStub remains a stub; full session list rendering deferred to Story 3.3+

### Improvements Checklist

- [x] Moved `todayISO` to module level (`SessionUploadModal.tsx`)
- [x] Added server-side file size validation (`route.ts`)
- [x] Removed redundant double-close in `SessionTimelineStub.tsx`
- [ ] Add timestamp/UUID prefix to Storage filename to prevent collision on duplicate uploads (`transcripts/${userId}/${sessionId}/${Date.now()}-${file.name}`) — `route.ts:166`
- [ ] Add Escape key handler to close modal — `SessionUploadModal.tsx`
- [ ] Story 3.3+: Replace `SessionTimelineStub` with real session list showing "Processing…" cards after upload (completes AC12)

### Security Review

Auth check → user resolution → ownership verification chain intact. Service role key used server-side only. Server-side file size guard added (was client-only before). No hardcoded secrets. One minor gap: MIME type not validated server-side (filename extension only) — acceptable for current risk profile as content is stored, not executed.

### Performance Considerations

Entire file loaded into memory as `ArrayBuffer` before Storage upload — acceptable up to 25MB at current scale. Two DB ops per upload (insert + update for file URL) is a minor inefficiency; could be collapsed by pre-generating a session ID client-side, but not worth optimising now.

### Files Modified During Review

- `apps/web/src/components/sessions/SessionUploadModal.tsx`
- `apps/web/src/app/api/sessions/route.ts`
- `apps/web/src/components/clients/SessionTimelineStub.tsx`

### Gate Status

Gate: PASS → docs/qa/gates/3.2-manual-transcript-upload.yml

### Recommended Status

✓ Ready for Done
