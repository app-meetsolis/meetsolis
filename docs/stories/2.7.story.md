# Story 2.7: Manual Notes Editor

## Status
Ready for Review

## Story

**As a** executive coach,
**I want** to add and edit free-form notes on a client record,
**so that** I can capture context, observations, and information that doesn't fit into session transcripts.

## Story Context

**Existing System Integration:**
- Integrates with: `PUT /api/clients/[id]`, `GET /api/clients/[id]`
- Technology: Next.js 14 App Router, React Query, Tailwind, Shadcn UI, sonner toast
- Follows pattern: existing ClientForm, DeleteClientDialog patterns
- Touch points: `packages/shared/src/schemas/client.ts` (ClientUpdateSchema already has `notes`), `clients.notes TEXT` column (migration 015)

**Dependency note:** Story 2.6 (full client detail view) was paused and never merged. This story creates a **minimal** `/clients/[id]` page stub sufficient to host the notes editor. Story 2.6 will expand this page later with session timeline, action items, etc.

## Acceptance Criteria

1. Client detail page exists at `/clients/[id]` — minimal stub: client name (h1), goal (subtext), "Coaching since" date, back link to `/clients`
2. Notes section displayed on the page (below client header)
3. Notes rendered as markdown when in view mode (bold, italic, lists, headings)
4. "Edit" button (or click empty state) switches to editable textarea (markdown input)
5. Notes auto-save on blur OR after 2-second debounce — no explicit save button needed
6. "Saving..." indicator during save; sonner toast "Notes saved" on success; toast on error
7. Notes persisted to `clients.notes TEXT` via `PUT /api/clients/[id]`
8. Notes survive page refresh (fetched via `GET /api/clients/[id]`)
9. Character limit: 10,000 chars with live counter displayed
10. Empty state: "Click to add notes about this client" — clicking enters edit mode
11. Mobile responsive

## Tasks / Subtasks

- [x] **Task 1: Minimal Client Detail Page** (AC: 1)
  - [x] Create `apps/web/src/app/(dashboard)/clients/[id]/page.tsx`
  - [x] Clerk auth check, redirect if unauthenticated
  - [x] UUID validation for `id` param
  - [x] Fetch client via `GET /api/clients/[id]` (React Query)
  - [x] Render: client name (h1), goal (muted text), "Coaching since [start_date]" (if set)
  - [x] Back link → `/clients`
  - [x] Loading skeleton + 404 error state
  - [x] Edit client button → opens existing `ClientModal` in edit mode

- [x] **Task 2: NotesEditor Component** (AC: 3–10)
  - [x] Create `apps/web/src/components/clients/NotesEditor.tsx`
  - [x] Props: `clientId: string`, `initialNotes: string | null`
  - [x] View mode: render markdown via `react-markdown` (already installed)
  - [x] Edit mode: `<textarea>` with tailwind styling
  - [x] Toggle: "Edit" button (view→edit), "Done" or blur (edit→view)
  - [x] Character counter: `{count}/10,000` shown in edit mode; disable further input at limit
  - [x] Auto-save: 2-second debounce on content change + save on blur
  - [x] Save state: "Saving..." text indicator during in-flight request
  - [x] On success: sonner toast "Notes saved"
  - [x] On error: sonner toast "Failed to save notes"
  - [x] Empty state (no notes): show "Click to add notes about this client" placeholder; click → enter edit mode

- [x] **Task 3: Wire into Client Detail Page** (AC: 2)
  - [x] Import `NotesEditor` into `/clients/[id]/page.tsx`
  - [x] Pass `clientId` and `client.notes` as props
  - [x] Section heading "Notes" above editor

- [x] **Task 4: API — enforce notes limit** (AC: 7, 9)
  - [x] Add `.max(10000)` to `notes` field in both `ClientCreateSchema` and `ClientUpdateSchema` in `packages/shared/src/schemas/client.ts`
  - [x] API returns 400 if notes exceeds 10,000 chars

## Dev Notes

- `notes` column: `TEXT`, nullable — already in DB via migration 015 ✅
- `ClientUpdateSchema` and `ClientCreateSchema` already include `notes` field — just need max length
- Use `react-markdown` for rendering (lightweight, no Tiptap needed)
- Auto-save: `useEffect` + `setTimeout`/`clearTimeout` debounce pattern — no extra library needed
- `PUT /api/clients/[id]` sends only `{ notes }` (partial update, not full client object)
- Toast: use `sonner` (already installed — `import { toast } from 'sonner'`)
- ClientCard: already navigates to `/clients/[id]` on click ✅ — no changes needed

## Risk & Compatibility

- **Primary Risk:** Minimal client detail page must not break existing `/clients` list or ClientCard
- **Mitigation:** New route only — no changes to existing components
- **Rollback:** Delete `/clients/[id]/page.tsx` and `NotesEditor.tsx`

## Definition of Done

- [x] Client detail page loads at `/clients/[id]`
- [x] Notes view/edit/save cycle works end-to-end
- [x] Auto-save fires on blur and after 2s debounce
- [x] 10,000 char limit enforced (UI + API)
- [x] Empty state and loading/error states present
- [x] Mobile responsive
- [x] No regression on `/clients` list page (pre-existing test failures confirmed unrelated)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-07 | 1.0 | Initial draft | Sarah (PO) |
| 2026-03-08 | 2.0 | Revised: self-contained with minimal detail page stub; simplified to react-markdown; 10k limit | John (PM) |
| 2026-03-08 | 3.0 | Implementation complete | James (Dev) |

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Completion Notes
- `react-markdown` was already installed (v^10.1.0) — no new install needed
- ClientCard already had `router.push('/clients/${client.id}')` — no changes needed
- Pre-existing test failures (20 failing) confirmed via git stash verification — all failures predate this story
- TypeScript: clean (`tsc --noEmit` passes)
- ESLint: clean on new files

### File List
- `apps/web/src/app/(dashboard)/clients/[id]/page.tsx` — NEW
- `apps/web/src/components/clients/NotesEditor.tsx` — NEW
- `packages/shared/src/schemas/client.ts` — MODIFIED (added max(10000) to notes fields)
- `docs/stories/2.7.story.md` — MODIFIED

## QA Results

*To be populated after implementation.*
