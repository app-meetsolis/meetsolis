status: done
date: 2026-05-09 13:00
source: QA gate 5.3 — non-blocking items

## Items

- [x] Add user-facing toast on checkout failure in `handleProCTA` — was silent console.error only (`NoveraPricing.tsx`)
- [x] Remove dead `cta: 'Start Free Trial'` field from Pro PLANS entry — overridden by button JSX (`NoveraPricing.tsx`)
- [x] Fix `/contact` link in NoveraFAQ — route did not exist; replaced with `mailto:hari@meetsolis.com` (`NoveraFAQ.tsx`)
- [x] Use `priceSpecification` + `UnitPriceSpecification` in JSON-LD instead of bare `billingIncrement` on `Offer` (`pricing/page.tsx`)
